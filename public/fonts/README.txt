Place `Cairo-Regular.ttf` in this folder if you want Arabic PDF export to use the Cairo font.

Expected path: /public/fonts/Cairo-Regular.ttf (served as `/fonts/Cairo-Regular.ttf`).

If you don't add the file, the exporter will try the remote GitHub URL and then fall back to Helvetica.
